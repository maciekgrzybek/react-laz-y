import React from 'react';

const Fallback = () => {
  const styles = {
    width: '100%',
    height: '100%',
    fontSize: '0.85rem',
  };

  return <div style={styles}>Loading component...</div>;
};

export default Fallback;
